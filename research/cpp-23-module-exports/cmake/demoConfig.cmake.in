# demoConfig.cmake
# Config file for find_package(demo CONFIG).
# Auto-generated by configure_package_config_file.

@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Add any find_package() calls needed by the imported targets here.
# (none for now -- demo::core is a header-only / module-only library)

# Include the targets exported by the build system.
include("${CMAKE_CURRENT_LIST_DIR}/demo-targets.cmake")

check_required_components(demo)
